# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
docs/

# Tauri updater signing keys — the private key must never be committed.
# Public half goes in tauri.conf.json (plugins.updater.pubkey); private half
# lives only in GitHub Actions secrets (TAURI_SIGNING_PRIVATE_KEY).
*.key
*.key.pub

# Editor / tooling directories
.vscode
.claude
.idea

# Claude session notes (local-only context)
CLAUDE.md
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
